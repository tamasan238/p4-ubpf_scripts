pipeline {
    agent any

    stages {
        stage('Terminate') {
            steps {
                build job: 'P4Shield - cloud-2/terminate'
            }
        }
        stage('Start') {
            steps {
                build job: 'P4Shield - cloud-2/start'
            }
        }
    }
}

